# ServerPnL

**完整名称**: `ATAS.DataFeedsCore.ServerPnL`
**类型**: 类

## 公共方法

  - `override string ToString()`
    - Returns a string that represents the current object.

## 属性

  - `DateTime Date { set; }`
  - `decimal EmulationPnL { set; }`
  - `decimal EmulationCommission { set; }`
  - `decimal RealPnL { set; }`
  - `decimal RealCommission { set; }`
